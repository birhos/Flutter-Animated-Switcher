name: flutterAnimatedSwitcher
description: A new Flutter project.

version: 1.0.0+1

environment:
  sdk: ">=2.8.4 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^0.1.3
  mobx: ^1.2.1+1
  flutter_mobx: ^1.1.0+1

dev_dependencies:
  flutter_test:
    sdk: flutter

  build_runner: ^1.10.0
  mobx_codegen: ^1.1.0+1

flutter:
  uses-material-design: true

  assets:
    - assets/images/
